Contributing
============

Planned content:

- Running the unit test suite (``python -m unittest tests.tests``)
- Code style expectations and reusable patterns (see ``.github/copilot-instructions.md``)
- Guidelines for adding new analysis modules
