# fastmdxplorer → fastmdxplora

**FastMDXplorer has been renamed to [FastMDXplora](https://github.com/aai-research-lab/FastMDXplora).**

This package is a redirect that installs `fastmdxplora` for you. Please migrate:

```bash
pip install fastmdxplora
```

```python
import fastmdxplora
```

The CLI command (`fastmdx`) is unchanged.
