version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

sphinx:
  configuration: docs/source/conf.py
  fail_on_warning: false

python:
  install:
    - method: pip
      path: .  # This installs your fastmdanalysis package with all dependencies
    - requirements: docs/requirements.txt  # Documentation-specific dependencies

formats:
  - pdf